# Use the README file as a reference to customize settings.

Limit:
  fields:
    default: wallet_type, value, currency
    form: value, currency, wallet_type
  relationships: 
  application: Application
