# Use the README file as a reference to customize settings.

Session:
  fields:
    default: created_at, SessionId, TimeToDie, profile_id, application_id
    form: SessionId, TimeToDie, profile_id, application_id
  relationships: 
  application: Profiles
  search: profile_id
  order_by: -created_at
